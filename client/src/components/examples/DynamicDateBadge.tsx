import DynamicDateBadge from '../DynamicDateBadge';

export default function DynamicDateBadgeExample() {
  return (
    <div className="flex items-center justify-center p-8">
      <DynamicDateBadge />
    </div>
  );
}
